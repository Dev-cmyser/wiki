namespace $ {
    export class $pixelbattle_workspace extends $mol_view {
        // No additional logic for now; the workspace just hosts the Board.
        // (We can add pan/zoom handlers here in the future.)
    }
}
